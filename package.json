{
    "private": true,
    "description": "Stub package manifest so Laravel Forge default deploy scripts (npm ci && npm run build) succeed. This app has no frontend build — styling is inline in Blade templates.",
    "scripts": {
        "build": "node -e \"console.log('No frontend build required — skipped')\"",
        "dev": "node -e \"console.log('No frontend dev server — use php artisan serve')\""
    }
}
